## Progress Log

### 2026-04-08 — Phase 1: Core App (Swift v0)
- Created Swift Package with CritterbarCore library + Critterbar executable
- Menu bar icon, transparent overlay windows, random wandering critters
- 18 unit tests, AppleScript e2e test, smoke test mode

### 2026-04-09 — Phase 2: Sniff Animation
- Added `@keyframes sniff-wiggle` CSS animation (side-to-side ±4px, 0.4s loop)
- Applied to `.critter[data-state="sniffing"]` in style.css
- Added e2e test verifying `animationName === "sniff-wiggle"` when critter is sniffing
- Verification: 19 unit tests + 9 e2e tests, all passing

### 2026-04-09 — Phase 2: More Critter Types
- Added 5 new critter types: rabbit 🐰, hamster 🐹, fox 🦊, frog 🐸, turtle 🐢
- Speed hierarchy: turtle(5) < hamster(9) < cat(12) < frog(14) < dog(17) < rabbit(20) < bird(22) < fox(30)
- Updated lib.rs tray menu with Add Rabbit/Hamster/Fox/Frog/Turtle items
- Fixed unit test speed expectations (cat/dog/bird were stale at 25/35/45; corrected to 12/17/22)
- Added emoji, speed, and fox-fastest/turtle-slowest unit tests for all 8 types
- Updated oscillation tests to cover all 8 critter types
- Verification: 20 unit tests + 9 e2e tests, all passing

### 2026-04-09 — Phase 2: One Critter Per Type
- Added `hasType(type)` to CritterManager to check for existing critter type
- Frontend deduplication: `addCritter` is a no-op if that type already exists
- Tauri command `set_critter_active(critterType, active)` disables/re-enables tray menu items
- Stored `MenuItem` clones in managed `MenuItems` state (Mutex<HashMap>) for live updates
- Frontend calls `invoke("set_critter_active", ...)` after add/removeAll (guarded by `__TAURI_INTERNALS__`)
- Added unit test `hasType` to critterManager test suite
- Verification: 21 unit tests + 9 e2e tests, all passing

### 2026-04-09 — Phase 2: Individual Critter Remove
- Added `removeCritterByType(type)` to CritterManager; returns removed Critter or undefined
- Added `removeCritter(type)` to main.ts: removes DOM element, manager entry, re-enables Add item
- Frontend exposes `window.critterbar.removeCritter(type)` for Playwright and Tauri events
- lib.rs: Added "Remove Cat 🐱" … "Remove Turtle 🐢" menu items (disabled by default)
- Menu restructured: Remove items (top, disabled) → separator → Add items → separator → Remove All → separator → Quit
- `set_critter_active` now also toggles the corresponding "remove-X" item (enabled when active)
- Tauri emits "remove-critter" event with critter type when a Remove item is clicked
- Added 2 unit tests for removeCritterByType (happy path + missing type)
- Verification: 23 unit tests + 9 e2e tests, all passing

### 2026-04-09 — Tauri v2 Rewrite
- Rewrote entire app from Swift/AppKit to Tauri v2 + TypeScript + Vite
- Ported critter logic: edge-walking, sniff pauses, corner nudge prevention
- Rust backend: system tray menu, transparent click-through window, no dock icon
- DOM-based renderer with data attributes for Playwright assertions
- 17 Vitest unit tests for critter model and manager
- 7 Playwright e2e tests with video recording (tests run against Vite dev server)
- verify.sh runs build + unit tests + e2e pipeline
- Ralph loop scripts (ralph-once.sh, afk-ralph.sh) wired to verify.sh
- install.sh builds and copies .app to /Applications

### 2026-04-09 — Phase 2: Tray Title Count
- Added `set_tray_title(title)` Tauri command; uses `app.tray_by_id("main")` to update tray title live
- TrayIconBuilder now uses `with_id("main")` and `.title("🐾")` as initial title
- Frontend calls `updateTrayTitle()` in `addCritter`, `removeCritter`, and `removeAll`
- Title shows "🐾 N" when N > 0, or "🐾" when no critters are active
- Verification: 26 unit tests + 11 e2e tests, all passing

### 2026-04-09 — Phase 2: Add Random
- Added `availableTypes()` to CritterManager: returns all critter types not currently on screen
- Added `addRandom()` to main.ts: picks a random available type and adds it; no-op if all 8 types active
- Exposed `addRandom` on `window.critterbar` API for Playwright and Tauri events
- Added "Add Random 🎲" menu item in lib.rs; disabled via `set_add_random_enabled` command when all types active
- Rust emits "add-random" event on click; frontend handles it like any other add
- Added 3 unit tests for `availableTypes` (empty manager, partial, full)
- Added 2 e2e tests: addRandom picks unused type, addRandom is no-op when all active
- Verification: 26 unit tests + 11 e2e tests, all passing

### 2026-04-09 — Phase 3: Variable Speed
- Added `baseSpeed` (readonly) and private `speedTimer`/`nextSpeedChangeIn` fields to Critter
- Speed multiplier chosen from [0.5, 0.75, 1.0, 1.0, 1.0, 1.25, 1.5] (weighted towards normal)
- Speed changes every 5-15s of simulated time, independent of sniff state
- Added 2 unit tests: "speed varies over simulated time" and "speed stays within 0.5x–1.5x of base speed"
- Verification: 28 unit tests + 11 e2e tests, all passing

### 2026-04-09 — Phase 2b: Persistent Panel (tray menu stays open)
- Replaced native left-click tray menu with a custom floating panel window (`panel.html`)
- Panel shows add buttons for all 8 critter types (disabled when active), Random, Add All, individual remove buttons for active critters, Remove All, and Quit
- Panel toggles open/closed on tray left-click; right-click still shows native context menu
- Panel positions itself below the tray icon using logical coordinates (scale-factor aware)
- Panel auto-resizes its window height to fit content via `resize_panel` Tauri command
- Panel state stays in sync with overlay via `critter-state-changed` events emitted by `set_critter_active`
- `get_active_critters` command reads enabled/disabled state of remove-X menu items
- `panel_action` command routes add/remove/quit actions from panel to the overlay
- Vite configured as multi-page build: `index.html` (overlay) + `panel.html` (panel)
- Added e2e test verifying panel renders 12 buttons in non-Tauri (Playwright) context
- Verification: 28 unit tests + 13 e2e tests, all passing

### 2026-04-09 — Phase 2b: Emoji Rotation
- Added CSS `rotate` individual property rules per `data-edge` attribute in style.css
- bottom=0deg, right=90deg, top=180deg, left=270deg — feet always face the screen edge
- CSS `rotate` property composes independently with `transform`, so sniff-wiggle animation is unaffected
- Added e2e test "critter rotation matches its edge" that samples edge+computed rotate style 5 times
- Verification: 28 unit tests + 12 e2e tests, all passing

### 2026-04-09 — Persist Active Critters Between Launches
- Added `saveActiveCritters()` in main.ts: writes `JSON.stringify(activeTypes)` to localStorage key `activeCritters`
- Added `restoreActiveCritters()` in main.ts: reads localStorage on startup and calls `addCritter` for each saved type
- `saveActiveCritters()` is called in `addCritter`, `removeCritter`, and `removeAll` to keep the store in sync
- `restoreActiveCritters()` is called once after `setupTauriEvents()` at startup
- localStorage persists across Tauri WebView restarts automatically — no plugin needed
- Added e2e test "active critters are restored after page reload": adds cat+dog, reloads page, asserts both are visible
- Verification: 28 unit tests + 14 e2e tests, all passing

### 2026-04-09 — Panel Auto-Hide on Blur
- Added `on_window_event` listener on the panel `WebviewWindow` in lib.rs
- When `WindowEvent::Focused(false)` fires, the panel is hidden automatically
- Covers clicking outside the panel (desktop, other app windows) to dismiss it
- No new tests needed: this is native OS window behaviour, not exercisable via Playwright
- Verification: 28 unit tests + 13 e2e tests, all passing

### 2026-04-09 — Launch at Login
- Added `tauri-plugin-autostart = "2"` dependency to Cargo.toml (MacosLauncher::LaunchAgent)
- Added `get_launch_at_login` and `set_launch_at_login` Tauri commands in lib.rs
- Plugin registered with `.plugin(tauri_plugin_autostart::init(...))` in the builder chain
- Panel UI: added "Launch at Login" toggle button in footer (✓ checkmark style when enabled)
- `init()` in panel.ts fetches initial autostart state alongside active critters via Promise.all
- Updated e2e panel button count assertion: 12 → 13 (added Launch at Login button)
- Verification: 30 unit tests + 14 e2e tests, all passing

### 2026-04-09 — Spread Spawning
- `CritterManager.addCritter` now uses spread spawning when at least one critter already exists
- `bestSpawnPoint(bounds)` samples 100 candidate positions around the full screen perimeter
- Picks the position that maximises min-distance to any existing critter (furthest from all critters)
- First critter still uses random edge placement (no existing critters to spread from)
- Added 2 unit tests: second critter >800px from forced bottom-left first critter; third critter >500px from both occupied corners
- Verification: 30 unit tests + 14 e2e tests, all passing

### 2026-04-09 — Persist Critter Positions
- `saveActiveCritters()` now saves full state per critter: `{ type, x, y, edge, movingForward }` (was type-name array only)
- `restoreActiveCritters()` reads new format and calls `manager.addCritterAtPosition(type, x, y, edge, movingForward)` to restore exact positions — bypasses spread-spawn logic
- Legacy format (plain string array) still handled gracefully for old saves
- Added `addCritterAtPosition(type, x, y, edge, movingForward)` to `CritterManager` — places critter at specific position without spread-spawn
- Added 2 unit tests: restores exact position/edge/direction; does not apply spread spawning
- Verification: 32 unit tests + 14 e2e tests, all passing

### 2026-04-09 — Panel State Sync on Restore
- Root cause: `get_active_critters` read from `remove-{type}` menu items that were never added to `item_map`; always returned `[]`
- Added `ActiveCritters(Mutex<HashSet<String>>)` managed state in Rust
- `set_critter_active` now updates the HashSet in addition to emitting `critter-state-changed`
- `get_active_critters` now reads from the HashSet (always returns correct set of active critters)
- Tray left-click handler now emits `panel-opened` after showing the panel
- `panel.ts` listens for `panel-opened` and re-fetches `get_active_critters` to sync state; eliminates startup race condition
- Result: panel checkboxes, Remove All, and Add All are correct immediately on first open after restore
- Verification: 30 unit tests + 14 e2e tests, all passing

### 2026-04-09 — Auto-Restart After Update
- `checkForUpdates` now calls `relaunch()` immediately after `downloadAndInstall()` completes
- Removed `updateReady` flag and `handleUpdateMenuClick` two-step flow
- `listen("check-update")` now calls `checkForUpdates(false)` directly
- Silent startup check also auto-restarts if an update is found (critters persist across restart)
- No new tests needed: update flow requires a live update server, not exercisable via Playwright
- Verification: 32 unit tests + 14 e2e tests, all passing

### 2026-04-09 — Critter Names on Hover
- Added `NAME_POOLS` per critter type (8 themed names each) and `generateName(type)` to `critter.ts`
- Added `name: string` field to `Critter`; generated in constructor from pool (or supplied explicitly for restore)
- Updated `SavedCritterState` in `main.ts` to include `name`; `saveActiveCritters` persists name, `restoreActiveCritters` passes it to `addCritterAtPosition`
- Updated `addCritterAtPosition` in `CritterManager` to accept optional `name` parameter
- `renderer.ts` now sets `el.title = critter.name` and `el.dataset.critterName` on each critter element
- Changed `.critter { pointer-events: none }` to `pointer-events: auto` so browser hover events fire and the `title` tooltip is shown (Tauri overlay uses OS-level `set_ignore_cursor_events`, so CSS pointer-events don't affect click-through)
- Added 3 unit tests: name is non-empty for every type; explicit name is preserved; `generateName` works for all types
- Added 2 e2e tests: `data-critter-name` attribute is set and non-empty; name is identical after page reload
- Verification: 38 unit tests + 16 e2e tests, all passing

### 2026-04-09 — Sleep State
- Added `sleeping` to `CritterState` union type: `{ kind: "sleeping"; remaining: number }`
- After a sniff ends, 20% chance critter falls asleep (10-20s duration) instead of resuming walking
- While sleeping, `update()` returns early — critter position and edge are frozen
- After sleep expires, critter returns to `walking` state with a fresh sniff timer
- Added `@keyframes zzz-float` CSS animation (gentle 4px vertical float, 2s loop)
- Added `::after` pseudo-element on `.critter[data-state="sleeping"]` showing 💤 emoji with float animation
- `renderer.ts` already sets `data-state` from `critter.state.kind` — no renderer changes needed
- Added 3 unit tests: eventually falls asleep; does not move while sleeping; wakes up after sleep duration
- Verification: 35 unit tests + 14 e2e tests, all passing
